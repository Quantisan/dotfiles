# NOTE: Codex writes to ~/.codex/config.toml (e.g., updating projects.<path>.trust_level),
# so this repo copy is reference only. Remember to sync any local edits manually.
model = "gpt-5.2-codex"
model_reasoning_effort = "medium"

# always ask for approval mode
approval_policy = "on-request"
